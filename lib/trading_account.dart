import 'package:flutter/material.dart';

class TradingAccount extends StatelessWidget {
  const TradingAccount({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}